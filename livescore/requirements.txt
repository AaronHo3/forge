# Score the Story — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core audio pipeline (always required) ──────────────────────────────
sounddevice>=0.4.6      # mic capture + speaker output (wraps PortAudio)
librosa>=0.10.0         # voice feature extraction (pitch, onset, centroid)
numpy>=1.24.0
scipy>=1.11.0           # real-time DSP filters in the audio callback

# ── Native real-time generation (the primary "python" mode) ────────────
# Magenta RealTime 2 via its MLX backend runs the model locally on Apple
# Silicon. This is the confirmed real-time path the app actually uses.
magenta-rt>=2.0.0
mlx>=0.29.4

# ── Semantic steering (optional "what they said" rail) ─────────────────
# Whisper transcribes narration; Claude maps it to musical style poles.
# The app degrades gracefully (music keeps playing) if these are absent
# or ANTHROPIC_API_KEY is unset.
openai-whisper>=20231117
anthropic>=0.40.0

# ── MIDI mode (legacy: drive the MRT2 AU plugin in a DAW) ───────────────
# Only needed for `--mode midi`. Not required for the native path above.
python-rtmidi>=1.5.0
