[pytest]
# The fallback/ modules use flat absolute imports (e.g. `from feature_mapper
# import ...`), so put this directory on sys.path for the test run.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    unit: pure-logic unit tests (no audio device, mic, model, or network)
